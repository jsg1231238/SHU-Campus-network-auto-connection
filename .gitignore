# 运行时 / 敏感文件，不纳入版本管理

# DPAPI 加密的校园网密码（本机专属，不外传）
.campuspwd

# 运行日志（含会话 token、密文）
campus-net-log.txt

# project-logger 回溯备份目录
_backup/

# Claude Code 内部工具目录（临时 worktree 等），不属于项目内容
.claude/

# 本地私有配置（真实 WiFi 名 / 学号），公开仓库不含个人信息
local.settings.ps1

# 浏览器抓包留下的个人网页快照与逆向分析参考资料，本地留存、不进仓库
上网认证.html
上网认证_files/
diag-portal.ps1
diag-out.txt
